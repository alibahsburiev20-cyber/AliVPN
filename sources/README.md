# Public VPN sources

Put public configuration list URLs here as plain text, one URL per line, when we expand the collector.

Do not treat public VPN nodes as trusted. The app should eventually test, score and filter nodes before use.
